import GovermentConfigs from "@/config/menu/goverment";
import { transformConfigToRoutes } from '@/util/util';

const GovermentRoutes = transformConfigToRoutes(GovermentConfigs)

export default GovermentRoutes;